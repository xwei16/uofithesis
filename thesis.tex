%! TEX program = lualatex
%! TEX root = thesis.tex

% DocumentMetadata is required for an accessible PDF
\DocumentMetadata{
    lang=en,
    pdfstandard=ua-2,
    tagging=on,
    tagging-setup={
        math/setup={mathml-AF,mathml-SE},
        extra-modules={verbatim-mo},
    }
}
% \documentclass{uofithesis}
\documentclass[thesis]{uofithesis} % for a thesis

\usepackage{fontsetup}

% If using math unicode-math is required for math accessibility.
% If not using math, remove this package.
\usepackage{unicode-math}

% Remove any graphics packages you are not using.
% Graphics packages can be used as long as alt-text is provided.
% For tikz alt text can be added with:
%   \begin{tikzpicture}[alt={Alt text...}]
% For images:
%  \includegraphics[alt={Alt text...}]{image.png}
\usepackage{graphicx}
\usepackage{subcaption}
\usepackage{tikz}
% uofithesis sets accessible colors for use in charts, but you can also define
% your own if needed
\usepackage{pgfplots}

\usepackage{csquotes}

% Bibliography for references
% biber must be set as the backend, style can be changed as needed
\usepackage[hidelinks,pdfusetitle]{hyperref}
\usepackage[backend=biber,style=ieee]{biblatex}
\usepackage[bottom]{footmisc}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{amsthm}
\newtheorem{theorem}{Theorem}[section]
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{corollary}[theorem]{Corollary}

% At time of writing, chemistry packages are not compatible with accessibility
% tagging, but there are no alternatives.
\usepackage[version=4]{mhchem}
\usepackage{chemfig}
\pgfplotsset{compat=1.18}

\addbibresource{./references.bib}

% Raggedright is recommended for accessibility
\raggedright
\setlength{\parindent}{1.5em}

\title{Robustness of Neural Networks for Computer Systems}
\author{Xinyi Wei}
\department{Computer Science}

% You can set a specific earned degree instead of Master/Doctor of Science/Philosophy
%\earneddegree{Doctor of Education}

% Add a concentration if applicable
%\concentration{Coffee Studies}
% Add a minor if applicable
%\minor{Global Studies}

\degreeyear{2026}

% You can set a custom text for the committee section, otherwise it will default
% to "Doctoral Committee:" or "Master's Committee:" based on the degree level
%\committeetitle{Advisers:}

\committee{
\item Assistant Professor Gagandeep Singh
}

\begin{document}

% Uncomment the copyrightpage macro to include an automatic copyright page
% before the title
%\copyrightpage
\maketitle

\abstract{
    % This is the abstract of the thesis. It should be a concise summary of the
    % research, including the main findings and conclusions. The abstract should
    % be no more than 350 words in length.

    Machine-learning-based Adaptive Bitrate (ABR) controllers, such as
    Pensieve~\cite{pensieve}, have demonstrated substantial Quality of
    Experience (QoE) improvements over hand-crafted heuristics in video
    streaming systems.  Yet their black-box neural nature introduces two
    intertwined problems: vulnerability to adversarial manipulation and
    the absence of formal correctness guarantees.  This thesis addresses
    both through a three-part framework that progresses from exposing
    vulnerabilities, to certifying the operating environment, to formally
    verifying closed-loop QoE properties.
 
    We first show that Pensieve is highly susceptible to
    specification-guided adversarial attacks.  Using pre- and
    post-condition specifications mined by
    \texttt{SpecTRA}~\cite{spec-tra}, we construct a Projected Gradient
    Descent (PGD) attack that perturbs only the most recent chunk
    download time by at most $10\%$.  This lightweight manipulation
    reduces the selected bitrate by $35$--$65\%$ and degrades
    single-round QoE by $30$--$40\%$ across $581$ successful attacks out
    of $30{,}000$ test states, while remaining within empirically valid
    network bounds.
 
    These vulnerabilities motivate the need for formal guarantees, which
    in turn require a trustworthy model of the environment in which the
    controller operates.  We address this by training a neural predictor
    $f^{\ast}$ for the ABR environment transition function~$f$ on
    real-world traces from the Puffer dataset using $19$ system features.
    Applying Clopper--Pearson confidence
    intervals~\cite{clopper-pearson}, our best model achieves a certified
    lower bound of $0.9846$ at $90\%$ confidence---at least $98.46\%$ of
    predictions incur less than $10\%$ normalised error---filling a
    critical gap in neural controller verification: a trustworthy model
    of the environment dynamics.
 
    Building on this certified predictor, we design an end-to-end QoE
    verification pipeline that reasons about system behaviour across
    multiple control rounds.  The pipeline alternates between a
    \emph{Pensieve phase}, in which
    \texttt{auto\_LiRPA}~\cite{auto-lirpa} bound propagation and
    parallel branch-and-bound search partition the input region into safe
    subregions where a single action provably dominates, and an
    \emph{environment phase}, in which $f^{\ast}$ propagates interval
    bounds forward to obtain guaranteed next-state bounds.  Iterating
    over multiple rounds builds a hierarchical reachability tree
    supporting formal reasoning about multi-step QoE properties of the
    complete neuro-symbolic control loop.  We further establish
    theoretical soundness guarantees and derive a statistical compounding
    bound characterising how environment prediction error accumulates
    across rounds.
 
    Together, these contributions establish a principled methodology for
    advancing neural systems controllers from empirical evaluation to
    formal safety certification.


}

\acknowledgements{
    % This page is optional. The text should be in either 10- or 12-point font
    % and the line spacing should be either 1.5 or double-spaced.

    % If you do not want to include an acknowledgment, delete the text of the
    % acknowledgment and the \texttt{\textbackslash acknowledgements} command
    % from your \texttt{.tex} file.

    I am deeply grateful to my advisor, Professor Gagandeep Singh, for his
    guidance, insight, and encouragement throughout this work. His vision
    for principled verification of neural-symbolic systems shaped every part of this
    thesis. Beyond his mentorship in research, Gagandeep has always been
    supportive of my career development and personal well-being, fostering
    an environment where I could grow both as a researcher and as a person.
    His generosity with time, constructive feedback at every stage of this
    project, and steadfast belief in my abilities have been a constant
    source of motivation. I could not have asked for a better advisor
    during my time at UIUC.

    I am also grateful to Debangshu Banerjee, Avaljot Singh, Calvin Xu,
    and the other members of the Formally Certified Automation and Learning
    (FOCAL) Lab at UIUC for creating a collaborative and intellectually
    stimulating environment. The conversations and shared experiences within
    the group made graduate school both more productive and more enjoyable.

    I am fortunate to have worked alongside exceptional collaborators
    throughout this research. I am especially thankful to Wanyu Zhao, whose
    insights---spanning both the big-picture direction of this work and its
    technical details---shaped my thinking in ways that are reflected
    throughout this thesis. I also thank Peizhe Liu and Gehan Jia, whose
    contributions to earlier stages of this research provided the foundation
    on which this thesis builds.

    I thank the Siebel School of Computing and Data Science at the
    University of Illinois Urbana-Champaign for providing an exceptional
    research environment and the resources that made this work possible.

    Finally, I thank my family and friends for their unwavering support
    throughout my graduate studies. Their encouragement has been a source
    of strength at every step of this journey.
}

\tableofcontents

% Sections make up the Main Text. Always use sectioning commands to create sections,
% subsections, etc. Do not use formatting commands to make regular text look like
% a section heading, as this will not be tagged properly for accessibility.
\section{Introduction}

    \label{chap:intro}
 
    Online video accounts for the dominant share of global Internet
    traffic~\cite{cloudinary-quality}.  To deliver smooth playback over
    volatile network links, nearly all modern streaming players employ an
    \emph{adaptive bitrate} (ABR) controller: a component that selects
    the video quality level for each chunk based on current network
    conditions, balancing video quality, rebuffering risk, and
    quality-switch penalties into a single Quality of Experience (QoE)
    objective~\cite{pensieve}.
 
    For decades, ABR controllers were built from hand-crafted heuristics.
    Rule-based algorithms such as BOLA~\cite{bola} and
    MPC~\cite{mpc} are simple and interpretable, but they leave
    substantial QoE improvements unrealised because they cannot capture
    the complex, nonlinear interactions among bandwidth variability,
    buffer dynamics, and chunk sizes.  Pensieve~\cite{pensieve} addresses
    this gap with a neural network controller trained via reinforcement
    learning (RL) to maximise QoE-related rewards directly, achieving
    gains of $10$--$70\%$ over rule-based baselines.  Other ML-based
    approaches, such as Fugu~\cite{fugu}, combine a supervised throughput
    predictor with model-predictive control to similar effect.
 
    This performance advantage comes at a fundamental cost.  Neural
    networks are opaque: their decision boundaries are encoded in
    millions of floating-point parameters, making their behaviour under
    worst-case inputs difficult to reason about or formally verify.  A
    decade of research in computer vision has shown that small, carefully
    crafted perturbations can cause neural networks to produce confident
    but incorrect outputs~\cite{attack}.  This thesis demonstrates that
    the same fragility afflicts neural systems controllers, and then
    develops the formal tools needed to reason about it.
 
 
    % ---- 1.1  Problem Statement and Challenges ----------------------------
    \subsection{Problem Statement and Challenges}
    \label{subsec:problem}
 
    The overarching goal of this thesis is to develop a principled
    framework for formally reasoning about the closed-loop QoE behaviour
    of neural ABR controllers.  Achieving this goal requires overcoming
    three challenges that go beyond classical neural network
    verification.
 
    \paragraph{Challenge~1: Absence of a correctness specification.}
    ABR streaming lacks a universal definition of correctness: different
    algorithms disagree under identical conditions, and no single QoE
    objective is accepted across deployments.  We address this by using
    SpecTRA~\cite{spec-tra} to mine pre-/post-condition specifications
    directly from Pensieve's training traces, grounding correctness in
    observed behaviour rather than an externally imposed standard.
 
    \paragraph{Challenge~2: Physical coupling of input features.}
    Pensieve's input features are not independent.  Throughput, for
    example, is definitionally chunk size divided by download time.
    Perturbations applied to one feature must respect these physical
    dependencies to remain realisable; otherwise, adversarial analyses
    and verification results describe states that can never arise in
    practice.  Both our attack construction
    (Chapter~\ref{chap:attack}) and our verification pipeline
    (Chapter~\ref{chap:verify}) enforce such consistency constraints.
 
    \paragraph{Challenge~3: Closed-loop environment dynamics.}
    Most critically, Pensieve operates in a \emph{closed loop}: each
    bitrate decision shapes the future network state the controller will
    observe.  The system evolves as
    \begin{equation}
        s_{t+1} = f(s_t,\, a_t), \qquad a_t = \mathrm{NN}(s_t),
        \label{eq:closed-loop}
    \end{equation}
    where $\mathrm{NN}$ is the neural controller and $f$ is the
    \emph{environment transition function}.  While $\mathrm{NN}$ can be
    analysed with existing verification tools, $f$ encodes stochastic
    network dynamics whose exact form is unknown.  Any formal guarantee
    about the \emph{system}---rather than the controller in
    isolation---requires a trustworthy, bounded model of $f$, a gap that
    prior work on neural-network-based systems (NN4Sys) verification has
    left unaddressed.
 
 
    % ---- 1.2  Contributions -----------------------------------------------
    \subsection{Contributions}
    \label{subsec:contributions}
 
    This thesis makes three contributions that together form an
    end-to-end framework for formally reasoning about Pensieve's
    closed-loop QoE behaviour.
 
    \paragraph{Adversarial vulnerability analysis
               (Chapter~\ref{chap:attack}).}
    We present the first systematic study of adversarial attacks on
    ML-based ABR controllers.  Using SpecTRA-derived specifications to
    constrain perturbations to physically valid states, we construct a
    PGD attack that increases the most recent download time by at most
    $10\%$.  Across $30{,}000$ test states, the attack finds $581$
    successful adversarial examples that reduce the selected bitrate by
    $35$--$65\%$ and degrade single-round QoE by $30$--$40\%$, while
    remaining within empirically valid network bounds and undetectable by
    coarse anomaly monitors.  These results establish a concrete threat
    model and motivate the need for principled formal defences.
 
    \paragraph{Statistically certified environment model
               (Chapter~\ref{chap:env}).}
    We construct a formally bounded neural predictor $f^{\ast}$ for the
    environment transition function~$f$, trained on real-world traces
    from the Puffer dataset~\cite{fugu}.  Applying Clopper--Pearson
    confidence intervals~\cite{clopper-pearson} to the empirical error
    distribution, our best model achieves a certified lower bound of
    $0.9846$ at $90\%$ confidence: at least $98.46\%$ of predictions
    incur less than $10\%$ normalised error.  This certified predictor
    fills the missing symbolic component that closed-loop verification
    requires, bridging the gap between neural controller analysis and
    the real-world environment dynamics it must account for.
 
    \paragraph{End-to-end QoE verification pipeline
               (Chapter~\ref{chap:verify}).}
    We design and implement a multi-round verification pipeline that
    alternates between two phases.  In the \emph{Pensieve phase},
    \texttt{auto\_LiRPA}~\cite{auto-lirpa} with CROWN-Optimised bound
    propagation and a parallel branch-and-bound search partition the
    input region into \emph{certified subregions}, each paired with an
    action: either a provably dominant action or, when dominance cannot
    be established, one of the remaining candidate actions (the same
    subregion is forked once per candidate).  In the
    \emph{environment phase}, the certified predictor $f^{\ast}$
    propagates interval bounds forward to obtain guaranteed next-state
    bounds.  Iterating for up to five control rounds builds a
    hierarchical reachability tree supporting formal reasoning about
    multi-step QoE properties of the complete neuro-symbolic control
    loop.  We further establish theoretical soundness guarantees and a
    statistical compounding bound quantifying how prediction error
    accumulates across rounds.
 
 
    % ---- 1.3  Structure ---------------------------------------------------
    \subsection{Thesis Structure}
    \label{subsec:structure}
 
    The remainder of this thesis is organised as follows.
    %
    Chapter~\ref{chap:background} provides technical background on ABR
    streaming, the Pensieve architecture, adversarial robustness, SpecTRA
    specification mining, environment modelling, neural network
    verification, and statistical certification.
    %
    Chapter~\ref{chap:attack} presents the adversarial attack study.
    %
    Chapter~\ref{chap:env} describes the certified environment model.
    %
    Chapter~\ref{chap:verify} presents the end-to-end verification
    pipeline and its theoretical guarantees.
    %
    Chapter~\ref{chap:eval} evaluates all three contributions
    experimentally.
    %
    Chapter~\ref{chap:discussion} discusses trade-offs, limitations, and
    practical implications.
    %
    Chapter~\ref{chap:related} surveys related work.
    %
    Chapter~\ref{chap:conclusion} concludes and outlines future
    directions.

\section{Background}
    \label{chap:background}
    
    This chapter reviews the technical foundations on which this thesis
    builds.
    Section~\ref{subsec:streaming} introduces ABR streaming and the
    Pensieve neural controller.
    Section~\ref{sec:bg-spectra} describes the SpecTRA
    specification-mining framework.
    Section~\ref{sec:bg-lirpa} covers neural network verification via
    LiRPA.
    Section~\ref{sec:bg-cp} reviews the Clopper--Pearson method for
    statistical certification.
    
    
    % ---------------------------------------------------------------------------
    \subsection{Video Streaming and the Pensieve Controller}
    \label{subsec:streaming}
    
    Online video has become the dominant load on the public Internet,
    accounting for more than 82\% of consumer IP traffic according to
    Cisco's Visual Networking Index~\cite{cloudinary-quality}.
    To keep playback smooth over volatile links, nearly all modern streaming
    players employ an ABR controller.
    Video content is delivered in discrete \emph{chunks}, each typically
    $2$--$4$~seconds of playback.
    After downloading each chunk, the client uses recent network
    metrics---throughput and buffer occupancy, among others---to select the
    quality level of the next chunk.
    
    Conventional ABR schemes such as \emph{BOLA}~\cite{bola} and
    \emph{MPC}~\cite{mpc} encode expert-designed rules; for example,
    ``maintain at least $B$~seconds of buffered video, then select the
    highest bitrate whose predicted download time is less than $B - C$,''
    where $B$ and $C$ are hand-tuned constants.
    While these heuristics perform reasonably under typical conditions, they
    struggle to capture the complex, nonlinear relationships among bandwidth
    variability, buffer dynamics, and chunk size.
    
    The primary objective of an ABR controller is to maximise QoE, which
    balances three factors: video quality (higher bitrates improve visual
    fidelity but increase download time and rebuffering risk), rebuffering
    (playback stalls severely degrade user experience), and quality switches
    (frequent bitrate changes are perceptually jarring).
    A standard QoE metric combines these factors as
    \begin{equation}
        \text{QoE}
            = \sum_t \text{bitrate}_t
            - \lambda_1 \cdot \sum_t \text{rebuffer time}_t
            - \lambda_2 \cdot \sum_t \lvert\Delta\text{bitrate}_t\rvert,
        \label{eq:qoe}
    \end{equation}
    where $\lambda_1$ and $\lambda_2$ are penalty weights that encode
    deployment-specific preferences.
    
    \paragraph{Pensieve.}
    Pensieve~\cite{pensieve} replaces hand-crafted heuristics with a neural
    policy trained via RL.
    At each decision point the player constructs a $6 \times 8$ state matrix
    $\mathbf{S}$ whose rows capture:
    (i)~past throughput samples,
    (ii)~past download times,
    (iii)~past buffer occupancy levels,
    (iv)~past selected bitrates,
    (v)~available chunk sizes at all quality levels, and
    (vi)~the number of chunks remaining until video end.
    Flattening this matrix yields a $48$-dimensional state vector that is
    fed into a two-layer fully connected neural network.
    The network outputs a softmax distribution over six discrete bitrate
    actions $a \in \{300,\, 750,\, 1200,\, 1850,\, 2850,\, 4300\}$~kbps.
    
    The model is trained to maximise a QoE-based reward
    \begin{equation}
        R = R_{\text{bitrate}}
            - 4.3 \cdot R_{\text{rebuffer}}
            - R_{\text{switch}},
        \label{eq:pensieve-reward}
    \end{equation}
    where $R_{\text{bitrate}}$ is the cumulative bitrate utility over recent
    chunks, $R_{\text{rebuffer}}$ is the total rebuffering time in seconds,
    and $R_{\text{switch}}$ is the sum of absolute differences between
    consecutive chunk bitrates.
    In empirical evaluations, Pensieve achieves QoE gains of $10$--$70\%$
    over traditional controllers~\cite{pensieve}, motivating widespread
    interest in ML-based ABR approaches.
    
    Crucially, Pensieve operates in a \emph{closed loop}: each selected
    action $a_t$ influences the next state $s_{t+1}$ through an environment
    transition function~$f$ (Equation~\ref{eq:closed-loop}).
    This closed-loop coupling means that any formal analysis of Pensieve
    must account not only for the neural controller itself but also for the
    dynamics of the environment in which it operates.
    
    
    % ---------------------------------------------------------------------------
    \subsection{Specification Mining with SpecTRA}
    \label{sec:bg-spectra}
    
    As noted in Section~\ref{subsec:problem}, ABR streaming lacks a
    universal correctness criterion.
    SpecTRA~\cite{spec-tra} fills this gap by mining
    \emph{pre-/post-condition specifications} directly from the controller's
    training traces.  Given logged input--output pairs from Pensieve,
    SpecTRA clusters the state space into operating regimes, extracts
    per-feature interval bounds $[\ell_j, u_j]$ as pre-conditions for each
    cluster, and summarises the dominant action $a^{\ast}$ as the
    post-condition.  A specification is thus a triple
    $(\text{cluster id},\; [\ell, u],\; a^{\ast})$: within the state
    region $[\ell, u]$, Pensieve is expected to select~$a^{\ast}$.
    
    These specifications serve two roles in this thesis: they constrain
    adversarial perturbations to physically plausible states
    (Chapter~\ref{chap:attack}), and they provide the initial input
    regions for the verification pipeline
    (Chapter~\ref{chap:verify}).
    
    
    % ---------------------------------------------------------------------------
    \subsection{Neural Network Verification with LiRPA}
    \label{sec:bg-lirpa}
    
    \emph{Linear Relaxation-based Perturbation Analysis} (LiRPA) is a
    family of methods for computing certified output bounds of neural
    networks over bounded input sets.
    Given an input region $[\ell, u] \subseteq \mathbb{R}^d$, LiRPA
    propagates linear relaxations of each layer's activation function
    through the network, producing sound over-approximations
    $[\underline{y},\, \overline{y}]$ of the true output range---that is,
    for every $x \in [\ell, u]$, the network output satisfies
    $\underline{y} \leq g(x) \leq \overline{y}$.
    
    The \texttt{auto\_LiRPA} library~\cite{auto-lirpa} implements several
    propagation strategies with different cost--tightness trade-offs:
    
    \paragraph{Interval Bound Propagation (IBP)}
    propagates axis-aligned interval bounds layer by layer.
    IBP is fast ($O(1)$ per neuron) but introduces substantial
    over-approximation at each non-linear activation, causing bounds to
    grow rapidly with network depth.
    
    \paragraph{CROWN~\cite{crown}}
    computes linear upper and lower bounding functions for each neuron by
    back-propagating affine relaxations from the output to the input layer.
    CROWN bounds are significantly tighter than IBP but require a backward
    pass for each output dimension.
    
    \paragraph{CROWN-Optimised (CROWN-Opt)}
    extends CROWN by treating the relaxation slopes of unstable ReLU neurons
    as learnable parameters optimised via gradient descent.
    This produces the tightest bounds among the three methods at the cost of
    multiple optimisation iterations.
    
    All three methods are \emph{sound}: the computed bounds provably contain
    the true output for every input in the region.
    In this thesis, LiRPA is the core bound-computation engine used in both
    the Pensieve verification phase and the environment verification phase
    of the pipeline described in Chapter~\ref{chap:verify}.
    
    
    % ---------------------------------------------------------------------------
    \subsection{Statistical Certification: Clopper--Pearson Intervals}
    \label{sec:bg-cp}
    
    The verification tools described in Section~\ref{sec:bg-lirpa} provide
    \emph{deterministic} soundness guarantees for neural network outputs.
    However, they require that every component in the pipeline be a
    differentiable neural network with known architecture and weights.
    The environment transition function $f$ does not satisfy this
    requirement: even though our neural approximation $f^{\ast}$ can be
    analysed via LiRPA, there is an irreducible gap between $f^{\ast}$ and
    the true dynamics~$f$.
    To quantify this gap rigorously, we turn to \emph{statistical
    certification}.
    
    The Clopper--Pearson method~\cite{clopper-pearson} provides exact
    binomial confidence intervals without parametric assumptions.
    Given $n$ i.i.d.\ test samples of which $k$ satisfy a binary success
    criterion, it models each sample as a Bernoulli trial with unknown
    success probability $p$ and computes
    \begin{equation}
        \mathrm{CI}_{1-\alpha}(p)
        = \left[
            \mathrm{Beta}^{-1}\!\!\left(\tfrac{\alpha}{2};\, k,\, n{-}k{+}1\right),\;\;
            \mathrm{Beta}^{-1}\!\!\left(1 - \tfrac{\alpha}{2};\, k{+}1,\, n{-}k\right)
        \right],
        \label{eq:cp}
    \end{equation}
    where $\alpha$ is the significance level and $\mathrm{Beta}^{-1}$ is
    the quantile function of the Beta distribution.
    The lower bound of this interval gives a conservative but rigorous
    guarantee on the true success probability~$p$.
    
    In Chapter~\ref{chap:env}, we define success as the event that
    $f^{\ast}$'s prediction incurs less than $10\%$ normalised error
    relative to the true next-state value.
    Applying the Clopper--Pearson method with $\alpha = 0.10$ yields a
    certified lower bound on the fraction of inputs for which $f^{\ast}$
    meets this accuracy threshold.
    This bound is then propagated into the multi-round verification pipeline
    (Chapter~\ref{chap:verify}), where a statistical compounding argument
    quantifies how prediction error accumulates across successive control
    rounds.

\section{Adversarial Vulnerability of Neural ABR Controllers}
\label{chap:attack}
 
    This chapter presents the first systematic study of adversarial attacks
    on ML-based ABR controllers.  We show that a lightweight,
    specification-guided perturbation of a single input feature is
    sufficient to induce significant QoE degradation in Pensieve,
    establishing a concrete threat model and motivating the formal
    verification framework developed in subsequent chapters.
    
    Section~\ref{sec:attack-prelim} reviews the adversarial robustness
    background needed for this chapter.
    Section~\ref{sec:attack-surface} characterises the attack surface of
    neural ABR controllers.
    Section~\ref{sec:threat-model} defines the threat model.
    Section~\ref{sec:attack-design} describes the specification-guided
    attack design.
    Section~\ref{sec:attack-eval} presents the experimental evaluation.
    Section~\ref{sec:attack-limitations} discusses limitations.
    Section~\ref{sec:attack-summary} summarises the chapter.
    
    
    % ---------------------------------------------------------------------------
    \subsection{Preliminaries: Adversarial Robustness and PGD}
    \label{sec:attack-prelim}
    
    Neural networks are known to be vulnerable to \emph{adversarial
    examples}: inputs that are imperceptibly perturbed yet cause the model
    to produce confident but incorrect outputs~\cite{attack}.
    In the image domain, a rich literature has developed around both attack
    algorithms---such as the Fast Gradient Sign Method (FGSM) and Projected
    Gradient Descent (PGD)~\cite{pgd}---and defences including adversarial
    training and certified robustness methods.
    
    PGD is an iterative first-order attack that computes adversarial
    perturbations by repeatedly taking gradient steps on the loss function
    and projecting the result back onto a prescribed perturbation set
    (typically an $\ell_p$-ball).
    Given input $x$, model $g$, and perturbation budget $\epsilon$, PGD
    iterates
    \begin{equation}
        x^{(k+1)} = \Pi_{\lVert \delta \rVert_p \leq \epsilon}
            \!\bigl(x^{(k)} + \alpha \cdot \mathrm{sign}\bigl(\nabla_x
            \mathcal{L}(g(x^{(k)}), y)\bigr)\bigr),
        \label{eq:pgd}
    \end{equation}
    where $\alpha$ is the step size, $\mathcal{L}$ is the loss, and
    $\Pi$ denotes projection onto the feasible set.
    
    While adversarial robustness has been studied extensively in
    classification settings, its implications for \emph{control} systems
    remain under-explored.
    In the ABR context, adversarial perturbations have a distinctive
    character: the attacker does not alter pixel intensities but rather
    manipulates observed network metrics (e.g.\ download times), and
    perturbations must remain physically realisable---for instance, they
    must respect the definitional coupling between throughput, chunk size,
    and download time.
    The remainder of this chapter develops a specification-guided PGD
    attack tailored to these constraints.
    
    
    % ---------------------------------------------------------------------------
    \subsection{Attack Surface of Neural ABR Controllers}
    \label{sec:attack-surface}
    
    Neural ABR policies expose a broad input attack surface, making them
    susceptible to manipulation at multiple layers of the network stack.
    We identify three representative adversary positions.
    
    \paragraph{Passive on-path adversary.}
    Because download time is measured at the client, any on-path
    device---such as a public Wi-Fi access point, a compromised home
    router, or an ISP-level traffic shaper---can inject jitter, delay
    packets, or disrupt acknowledgments to falsify perceived network
    performance.
    
    \paragraph{Active JavaScript adversary.}
    A malicious third-party advertisement running in the same browser tab
    can generate CPU contention or spawn background fetches that interfere
    with the ABR player's HTTP requests, degrading measured throughput.
    
    \paragraph{Content-side manipulator.}
    A compromised CDN edge server can alter server-side timestamps or
    response pacing to skew round-trip-time measurements, without modifying
    the encrypted video payload itself.
    
    None of these attack vectors requires access to the player binary or
    decryption of traffic.  By manipulating a small number of observable
    signals, an adversary can drive the neural controller toward poor
    bitrate decisions, leading to quality degradation that end users
    attribute to the streaming provider.  This asymmetric attribution makes
    ML-based ABR controllers especially attractive targets.
    
    
    % ---------------------------------------------------------------------------
    \subsection{Threat Model}
    \label{sec:threat-model}
    
    We consider a network-layer adversary capable of launching targeted
    Distributed Denial-of-Service (DDoS) attacks to degrade the
    performance of the ABR controller.  The attacker does not need to
    compromise the video player or the content delivery infrastructure;
    instead, it relies on disrupting network conditions to manipulate
    client-side observations.
    
    Potential targets for the DDoS attack include:
    \begin{itemize}
        \item \textbf{Video server:} introducing packet delays or throttling
            response times to inflate perceived download latency.
        \item \textbf{Home router or access link:} injecting congestion or
            cross-traffic near the user to induce higher variability in
            throughput and delay.
        \item \textbf{Other on-path infrastructure:} such as shared Wi-Fi
            access points, edge proxies, or ISP-level bottlenecks.
    \end{itemize}
    
    The attacker's primary goal is to increase the observed video download
    time for the most recent chunk.  By doing so, the attack steers the ABR
    policy toward lower bitrate selections, ultimately degrading QoE
    without needing to intercept or alter encrypted video content.
    
    
    % ---------------------------------------------------------------------------
    \subsection{Specification-Guided Attack Design}
    \label{sec:attack-design}
    
    This section describes the design of our adversarial attack pipeline.
    We first explain the role of specification guidance
    (Section~\ref{sec:attack-why-spec}), then detail two simplifications
    that enable tractable evaluation
    (Section~\ref{sec:attack-simplification}), describe how specifications
    are generated (Section~\ref{sec:attack-specgen}), and present the
    constrained PGD algorithm (Section~\ref{sec:attack-pgd}).
    
    
    % - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
    \subsubsection{Why Specification Guidance?}
    \label{sec:attack-why-spec}
    
    Unconstrained gradient-based attacks operate over the full input space
    and often generate physically invalid states---for example, negative
    throughput values or internal inconsistencies such as
    $\text{throughput} \times \text{download time} \neq \text{chunk size}$.
    Such states would either be rejected by sanity checks in the player or
    neutralised by built-in recovery logic.
    
    Incorporating SpecTRA-mined invariants into PGD transforms it into a
    \emph{specification-guided} attack, where each perturbation step is
    projected back onto the legal state space.  This design offers three
    advantages:
    \begin{itemize}
        \item \textbf{Plausibility.}  The resulting adversarial states
            closely resemble natural variations, making them harder to
            detect via simple anomaly checks.
        \item \textbf{Efficiency.}  The constrained search space reduces
            wasted gradient steps, accelerating convergence and avoiding
            updates that would be clipped away.
        \item \textbf{Generalisation.}  Staying within the manifold of
            realistic states increases the likelihood that adversarial
            examples remain effective when deployed in live systems with
            internal feature dependencies.
    \end{itemize}
    
    In short, specification-guided attacks combine the optimisation power of
    gradient methods with the realism needed to influence real-world
    controllers, bridging the gap between theoretical feasibility and
    practical exploitability.
    
    
    % - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
    \subsubsection{Simplifications}
    \label{sec:attack-simplification}
    
    We make two simplifications to enable tractable evaluation.
    
    \paragraph{Two-chunk video runs.}
    Instead of attacking an entire video session---where the number of
    chunks is user-dependent and varies in length---we treat a two-chunk
    segment as a complete video run.  This isolates the effect of a single
    adversarial perturbation on immediate bitrate selection and QoE.
    
    \paragraph{Exclusion of rebuffering.}
    Pensieve's dataset contains no rebuffering events across the sampled
    video runs, so our experiments omit this component from the QoE
    calculation.  Moreover, rebuffering behaviour is highly dependent on
    user interaction patterns (e.g.\ pause, seek, network switching), which
    are difficult to model accurately in a controlled attack scenario.
    
    Under these assumptions, the QoE formula for a video run becomes
    \begin{equation}
        \text{QoE}
            = q(R_1) + q(R_2) - \lvert q(R_1) - q(R_2) \rvert
            = 2 \cdot \min\{q(R_1),\, q(R_2)\},
        \label{eq:attack-qoe}
    \end{equation}
    where $R_1$ and $R_2$ are the bitrates selected for the two chunks and
    $q(\cdot)$ is a logarithmic utility function mapping bitrate to
    perceived quality:
    \begin{equation}
        q(R) = \log\!\left(\frac{R}{R_{\min}}\right),
        \label{eq:utility}
    \end{equation}
    with $R_{\min} = 300$~kbps denoting the minimum available bitrate in
    Pensieve.  This formulation captures the diminishing returns of higher
    bitrates: increasing bitrate improves quality, but the perceptual gain
    becomes less pronounced at higher values.
    
    
    % - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
    \subsubsection{Specification Generation}
    \label{sec:attack-specgen}
    
    We use SpecTRA~\cite{spec-tra} to automatically generate input and
    output specifications for Pensieve.  SpecTRA analyses logged training
    traces and extracts concrete pre-/post-condition bounds that serve two
    purposes in the attack pipeline: they define a valid state manifold,
    ensuring that perturbed inputs remain physically plausible and
    consistent with real-world network behaviour, and they restrict the
    optimisation space of PGD, enabling faster convergence and more stealthy
    perturbations.
    
    \begin{figure}[t]
        \centering
        % TODO: include the SpecTRA specification figure from the paper
        \includegraphics[width=0.85\linewidth,alt={Example SpecTRA
          specification showing pre-condition bounds on selected input
          features and post-condition constraints on output bitrate and
          QoE.}]{figures/spectra_example.png}
        \caption{Example specification generated by SpecTRA, showing
            pre-condition bounds on selected input features and
            post-condition constraints on output bitrate and QoE.}
        \label{fig:spectra-spec}
    \end{figure}
    
    For example, as shown in Figure~\ref{fig:spectra-spec}, SpecTRA
    provides pre-condition bounds on selected input features and
    post-condition constraints on output behaviour.  The pre-condition
    specifies that \texttt{Chunks\_left} must lie in the range $[0.0,\,
    9.6]$, \texttt{Last1\_buffer\_size} in $[11.6,\, 15.1]$,
    \texttt{Last1\_chunk\_bitrate} is fixed at $2850$~kbps, and
    \texttt{Last1\_downloadtime} lies in $[1.5,\, 7.9]$.  The
    post-condition defines allowable values for the controller's output
    bitrate and expected QoE, restricting the policy to a subset of
    possible actions and outcomes.  These constraints are encoded into the
    attack pipeline as projection bounds to ensure that all adversarial
    inputs remain within the feasible region.
    
    
    % - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
    \subsubsection{SpecTRA-Constrained PGD Attack}
    \label{sec:attack-pgd}
    
    We implement a targeted adversarial attack using PGD.  The goal is to
    apply minimal perturbations to the ABR state input in order to force
    the neural policy to select the lowest bitrate ($300$~kbps), thereby
    minimising QoE.
    
    At each step, PGD performs gradient ascent on the targeted loss
    function (cross-entropy with respect to the $300$~kbps class), followed
    by projection back onto a feasible region defined by both an
    $\ell_\infty$-norm budget and the physical bounds mined by SpecTRA.
    To maintain stealth and physical plausibility, we apply PGD to only one
    input feature: \texttt{Last1\_downloadtime}.
    
    The attack proceeds iteratively as shown in
    Algorithm~\ref{alg:pgd-attack}.  In each iteration, the value of
    \texttt{Last1\_throughput} is updated to remain consistent with the
    perturbed \texttt{Last1\_downloadtime}, preserving the relationship
    between throughput and download time and ensuring input realism.
    
    \begin{algorithm}[t]
        \caption{SpecTRA-Constrained PGD Attack}
        \label{alg:pgd-attack}
        \begin{algorithmic}[1]
            \Require Initial state $\mathbf{x}$, model $\pi_\theta$, step
                    size $\alpha$, perturbation bound $\epsilon$,
                    specification bounds $\mathbf{B}_{\text{low}}$,
                    $\mathbf{B}_{\text{high}}$, iterations $T$
            \Ensure  Adversarial state $\tilde{\mathbf{x}}^{(T)}$
            \State Initialise: $\tilde{\mathbf{x}}^{(0)} \gets \mathbf{x}$
            \For{$t = 0$ \textbf{to} $T - 1$}
                \State Compute gradient:
                    $\mathbf{g} \gets \nabla_{\tilde{\mathbf{x}}}
                        \mathcal{L}(\tilde{\mathbf{x}}^{(t)},\, y = 0)$
                \State Gradient step:
                    $\tilde{\mathbf{x}}^{(t+\frac{1}{2})} \gets
                        \tilde{\mathbf{x}}^{(t)} + \alpha \cdot
                        \mathrm{sign}(\mathbf{g})$
                \State Project to $\ell_\infty$ ball:
                    $\tilde{\mathbf{x}}^{(t+\frac{1}{2})} \gets
                        \mathrm{clip}\bigl(
                        \tilde{\mathbf{x}}^{(t+\frac{1}{2})},\;
                        \mathbf{x} - \epsilon,\;
                        \mathbf{x} + \epsilon\bigr)$
                \State Project to SpecTRA bounds:
                    $\tilde{\mathbf{x}}^{(t+1)} \gets
                        \mathrm{clip}\bigl(
                        \tilde{\mathbf{x}}^{(t+\frac{1}{2})},\;
                        \mathbf{B}_{\text{low}},\;
                        \mathbf{B}_{\text{high}}\bigr)$
            \EndFor
            \State \Return $\tilde{\mathbf{x}}^{(T)}$
        \end{algorithmic}
    \end{algorithm}
    
    This attack is simple, fast to compute, and leverages domain
    constraints to ensure that adversarial inputs are realistic and
    plausible under real network conditions.
    
    
    % ---------------------------------------------------------------------------
    \subsection{Evaluation}
    \label{sec:attack-eval}
    
    We evaluate the attack against the Pensieve ABR agent to assess its
    vulnerability to specification-guided adversarial perturbations.
    
    
    % - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
    \subsubsection{Experimental Setup}
    \label{sec:attack-setup}
    
    We evaluate the attack against a single SpecTRA-derived specification
    using $30{,}000$ test samples randomly generated from the
    specification.  A test sample is considered a \emph{successful attack}
    if the selected bitrate both degrades relative to the unperturbed
    decision and falls outside the nominal output range defined by the
    post-condition.
    
    
    % - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
    \subsubsection{Metrics}
    \label{sec:attack-metrics}
    
    We evaluate our attack using three primary metrics:
    \begin{itemize}
        \item \textbf{Attack success rate:} the percentage of states where
            the selected bitrate is both degraded and falls outside the
            nominal output range defined by the specification (i.e.\
            out-of-bound).
        \item \textbf{Average bitrate reduction:} the average decrease in
            selected bitrate across successfully attacked states, compared
            to the original, unperturbed decisions.
        \item \textbf{Average QoE loss:} the mean reduction in QoE
            resulting from the attack.
    \end{itemize}
    These metrics collectively capture both the effectiveness and
    perceptual impact of the attack on the ABR agent's decision-making.
    
    
    % - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
    \subsubsection{Results}
    \label{sec:attack-results}
    
    Out of $30{,}000$ test samples, $581$ ($1.9\%$) resulted in successful
    QoE downgrades---that is, the selected bitrate violated the output
    bounds defined by the specification and led to degraded quality.
    
    \begin{figure}[t]
        \centering
        \includegraphics[width=0.85\linewidth,alt={Violin plots showing
          proportional change in download time, bitrate, and QoE after the
          specification-guided PGD attack.}]{figures/attack_effectiveness.png}
        \caption{Proportional change in key metrics after the
            specification-guided PGD attack.  A perturbation of at most
            $10\%$ in \texttt{Last1\_downloadtime} (left) causes Pensieve to
            select markedly lower bitrates ($35$--$65\%$ reduction, centre),
            which in turn lowers overall QoE by $30$--$40\%$ (right).  Each
            violin plot shows the distribution of percentage change across
            all $581$ successful attacks; the dashed red line denotes
            ``no change.''}
        \label{fig:attack-violin}
    \end{figure}
    
    \begin{figure}[t]
        \centering
        \includegraphics[width=0.85\linewidth,alt={Histogram of QoE scores
          before and after the attack, showing a shift from 4.5 to
          2.7.}]{figures/attack_distribution.png}
        \caption{Histogram of QoE scores before and after the attack.  Blue
            bars represent the original Pensieve decisions; red bars show
            outcomes after the specification-guided PGD perturbation.  The
            majority of successful attacks shift the QoE mode from $4.5$ to
            $2.7$, indicating a substantial reduction in perceived streaming
            quality.}
        \label{fig:attack-qoe-hist}
    \end{figure}
    
    Figure~\ref{fig:attack-violin} illustrates the proportional deviation
    of key metrics between the attacked and original states.  While
    download time was increased by no more than $10\%$, this small change
    caused a substantial reduction in both bitrate ($35$--$65\%$) and QoE
    ($30$--$40\%$).
    
    Figure~\ref{fig:attack-qoe-hist} shows the distribution of QoE scores
    before and after the attack.  Most successful cases caused a drop in
    QoE from $4.5$ (original) to $2.7$ (attacked), reflecting a noticeable
    and user-visible degradation in perceived streaming quality.
    
    \subsubsection{Attack Stealth}
    \label{sec:attack-stealth}
    
    All perturbations are limited to a $10\%$ increase in download time,
    which falls within normal network variance and is unlikely to trigger
    anomaly detectors that monitor coarse throughput trends.  Because the
    perturbed states remain inside the SpecTRA-derived valid state manifold,
    they are indistinguishable from natural network fluctuations when
    assessed by standard sanity checks.
    
    
    % ---------------------------------------------------------------------------
    \subsection{Limitations}
    \label{sec:attack-limitations}
    
    While the attack demonstrates the feasibility of adversarial
    degradation in ML-based ABR systems, it is subject to several
    simplifying assumptions and scoped constraints.
    
    \paragraph{Narrow attack surface.}
    The attack modifies only one input feature:
    \texttt{Last1\_downloadtime}.  In real-world scenarios, adversaries may
    be able to manipulate multiple correlated features (e.g.\ throughput,
    buffer size), potentially yielding higher success rates and more
    nuanced attack vectors.
    
    \paragraph{Simplified QoE modelling.}
    We assume a fixed number of video chunks and exclude rebuffering from
    the QoE calculation.  This decision is based on the absence of
    rebuffering events in the Pensieve dataset, as well as the difficulty
    of modelling user-driven behaviours (e.g.\ pause, seek).  However, this
    abstraction limits realism and may underestimate certain types of
    degradation.
    
    \paragraph{Single specification constraint.}
    The evaluation uses a single specification derived from Pensieve's
    training trace.  This restricts the applicability of our findings to one
    particular operational regime.  Broader generalisation across multiple
    specifications, content types, and runtime environments remains
    unexplored.
    
    These limitations highlight key avenues for future work, including
    multi-feature attacks, more expressive QoE models, and wider validation
    across modern ABR systems and network conditions.  Importantly, the
    vulnerabilities exposed here---even under conservative
    assumptions---motivate the formal verification framework developed in
    the remainder of this thesis.
    
    
    % ---------------------------------------------------------------------------
    \subsection{Summary}
    \label{sec:attack-summary}
    
    This chapter presented the first systematic study showing that ML-based
    ABR controllers can be compromised by small, in-bound perturbations.
    Using SpecTRA to extract realistic specifications, we embedded those
    constraints in a PGD attack that modifies only one
    feature---\texttt{Last1\_downloadtime}---by at most $10\%$.  This
    lightweight manipulation is sufficient to drive Pensieve to select the
    lowest bitrate in $1.9\%$ of $30{,}000$ test states, reducing average
    bitrate by $35$--$65\%$ and QoE by $30$--$40\%$ across successful
    attacks.  Because the perturbations stay inside an empirically valid
    state manifold, they evade simple sanity checks, exposing a practical
    security risk for neural systems deployments.
    
    These findings establish two concrete motivations for the subsequent
    chapters.  First, the fact that Pensieve is vulnerable to small
    perturbations underscores the need for a trustworthy environment model
    that can support formal reasoning about worst-case behaviour
    (Chapter~\ref{chap:env}).  Second, the specification-guided attack
    framework provides the perturbation sets and correctness criteria that
    seed the end-to-end verification pipeline
    (Chapter~\ref{chap:verify}).


% =============================================================================
%  CHAPTER 4: A STATISTICALLY CERTIFIED ENVIRONMENT MODEL
%  Restructured from the CS 521 conference paper.
%  Content preserved; wording refined for thesis register.
% =============================================================================

\section{A Statistically Certified Environment Model}
\label{chap:env}

    Chapter~\ref{chap:attack} demonstrated that Pensieve is vulnerable to
    small, specification-guided perturbations.  Designing principled
    defences requires formal reasoning about the controller's closed-loop
    behaviour, which in turn requires a trustworthy model of the
    environment in which the controller operates.  This chapter constructs
    such a model: a neural predictor $f^{\ast}$ for the environment
    transition function~$f$, equipped with statistically certified error
    bounds.

    Section~\ref{sec:env-prelim} introduces the environment-modelling
    problem and the Puffer dataset.
    Section~\ref{sec:env-architecture} describes the neural network
    architecture and training procedure.
    Section~\ref{sec:env-certification} explains how we derive statistical
    certification guarantees via Clopper--Pearson bounds.
    Section~\ref{sec:env-eval} presents the experimental evaluation.
    Section~\ref{sec:env-summary} summarises the chapter.


    % ---------------------------------------------------------------------------
    \subsection{Preliminaries: Environment Modelling and the Puffer Dataset}
    \label{sec:env-prelim}

    Formal verification of a closed-loop system
    (Equation~\ref{eq:closed-loop}) requires a model of the environment
    transition function~$f$ that maps $(s_t, a_t) \mapsto s_{t+1}$.
    In the ABR setting, $f$ encodes stochastic network dynamics---bandwidth
    fluctuations, TCP congestion-control behaviour, CDN routing---whose
    exact form is unknown and varies across deployments.  While the neural
    controller $\mathrm{NN}$ can be analysed with existing verification
    tools, $f$ cannot: its dynamics must be learned from data and its
    fidelity to the true system must be established empirically.

    The \emph{Puffer} dataset~\cite{fugu} provides a large-scale source of
    real-world streaming telemetry suitable for this purpose.
    Puffer is a live television streaming platform operated by Stanford
    University that logs fine-grained per-chunk statistics---including
    throughput, download time, buffer level, and selected bitrate---for
    every session.
    Because the data is collected from a production system serving real
    users over real networks, it captures the full diversity of network
    conditions that an ABR controller encounters in practice.

    Among the quantities encoded in $f$, next-chunk download time is the
    most consequential for QoE: it governs buffer dynamics and, when
    underestimated, is the proximate cause of rebuffering stalls.  It is
    also the hardest to model, because it is determined by network
    conditions---bandwidth fluctuations, TCP behaviour, CDN routing---that
    are inherently stochastic and not observable in advance.  Existing ABR
    simulators address this by replaying recorded bandwidth traces, but
    they offer no theoretical guarantee that the simulated dynamics
    faithfully represent the true environment; a controller verified in
    such a simulator may behave differently on real networks where
    conditions deviate from the replay traces.  We therefore learn this
    component from the Puffer traces by training a neural network
    $f^{\ast}$ to predict next-chunk download time from a window of recent
    throughput and timing measurements.  Critically, rather than treating
    $f^{\ast}$ as an unverified black box, we derive formal statistical
    guarantees on its prediction accuracy via Clopper--Pearson confidence
    intervals, giving our verification pipeline an end-to-end correctness
    certificate that trace-replay simulators cannot provide.


    % ---------------------------------------------------------------------------
    \subsection{Environment Model Architecture}
    \label{sec:env-architecture}

    % - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
    \subsubsection{Dataset and Features}
    \label{sec:env-data}

    To train the environment predictor $f^{\ast}$, we use real-world ABR
    traces collected from the Puffer dataset~\cite{fugu}.  Each training
    sample represents a state--action pair and contains $19$ features:
    \begin{itemize}
        \item $8$ past bandwidth measurements (in Mbps),
        \item $8$ past download times (in seconds),
        \item $2$ past chunk sizes (in MB),
        \item $1$ upcoming chunk size (in MB).
    \end{itemize}
    These inputs form a $19$-dimensional feature vector used to predict the
    (normalised) next-chunk download time.  This empirical data provides a
    realistic basis for modelling environment transitions in ABR streaming
    under diverse network conditions.


    % - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
    \subsubsection{Neural Network Architecture}
    \label{sec:env-nn}

    The environment predictor $f^{\ast}$ is implemented as a fully
    connected neural network.  We experimented with both $2$-hidden-layer
    and $3$-hidden-layer architectures.  The output is a single linear
    regression value corresponding to the predicted next-chunk download
    time.  The network is trained using the Adam optimiser with L1 loss,
    which improves robustness to heavy-tailed download-time fluctuations
    common in real network traces.


    % - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
    \subsubsection{Normalisation}
    \label{sec:env-norm}

    All features and labels are normalised to the range $[0, 1]$ by
    dividing by their respective maximum values observed in the training
    set.  Training, evaluation, and certification are performed entirely in
    this normalised space.  This approach ensures numerical stability and
    allows error guarantees to be expressed in terms of relative prediction
    error, consistent with prior work on ABR trace modelling.


    % - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
    \subsubsection{Training Procedure}
    \label{sec:env-training}

    We trained two types of models to evaluate generalisation:
    \begin{enumerate}
        \item \textbf{Individual models:} $10$ models, each trained on
            $1{,}000$ samples from a single day and tested on a different
            $1{,}000$ samples from the same day.  These models evaluate
            in-distribution performance.
        \item \textbf{Combined model:} one model trained on $10{,}000$
            samples drawn from $10$ random days ($1{,}000$ per day) and
            tested on $9{,}000$ samples from another $9$ different random
            days.  This model evaluates generalisation across diverse
            network conditions.
    \end{enumerate}


    % ---------------------------------------------------------------------------
    \subsection{Statistical Certification via Clopper--Pearson Bounds}
    \label{sec:env-certification}

    To provide formal reliability guarantees, we employ Clopper--Pearson
    confidence intervals~\cite{clopper-pearson}, which offer exact binomial
    confidence bounds without parametric assumptions.  The general method is
    reviewed in Section~\ref{sec:bg-cp}; here we describe its concrete
    application to the environment predictor.

    \paragraph{Success criterion.}
    A prediction is considered a \emph{success} if it satisfies
    \begin{equation}
        \lvert f(x) - f^{\ast}(x) \rvert \leq \epsilon,
        \label{eq:env-success}
    \end{equation}
    where $f(x)$ is the ground-truth next-chunk download time,
    $f^{\ast}(x)$ is the predicted value, and $\epsilon = 0.10$ represents
    a $10\%$ tolerance in the normalised domain.  This threshold balances
    precision with practical robustness requirements for ABR systems.

    \paragraph{Confidence interval computation.}
    Given a test set of $n$ samples, suppose $k$ samples satisfy the
    success criterion.  We model each prediction as an independent
    Bernoulli trial with unknown success probability $p$.  Using the
    Clopper--Pearson method (Equation~\ref{eq:cp}), we compute an exact
    $90\%$ confidence interval for $p$ by setting $\alpha = 0.10$.  The
    lower bound of this interval provides our certified guarantee.

    \paragraph{Interpretation.}
    For our best model (the combined model), the $90\%$ Clopper--Pearson
    confidence interval on test-set accuracy is $[0.9846,\, 0.9887]$.  We
    use the lower bound as our certified guarantee:
    \begin{equation}
        \Pr\!\bigl(\lvert f(x) - f^{\ast}(x) \rvert \leq 0.10\bigr)
        \;\geq\; 0.9846
        \quad\text{with $90\%$ confidence.}
        \label{eq:env-certified}
    \end{equation}
    This conservative bound ensures that, with $90\%$ confidence, at least
    $98.46\%$ of future predictions will have less than $10\%$ normalised
    error under the i.i.d.\ assumption.

    \paragraph{Key assumption.}
    The validity of Clopper--Pearson bounds requires that test samples are
    independent and identically distributed (i.i.d.).  We satisfy this
    assumption by: (1)~randomly shuffling trace data before train--test
    splitting, and (2)~ensuring temporal independence by drawing samples
    from different streaming sessions and days.  While real network traces
    exhibit temporal correlations within sessions, our sampling strategy
    mitigates this concern by treating each chunk download as an independent
    observation.

    \paragraph{Providing a certified environment model.}
    The statistical guarantee derived from Clopper--Pearson bounds serves as
    the formal foundation for using $f^{\ast}$ in verification tasks.
    Unlike network simulators deployed without guarantees, our certified
    predictor comes with quantifiable reliability: we know, with high
    confidence, what fraction of predictions will be accurate.  This
    transforms $f^{\ast}$ from a heuristic model into a trustworthy
    symbolic component suitable for reasoning about closed-loop system
    behaviour and enabling principled verification of neural ABR
    controllers.


    % ---------------------------------------------------------------------------
    \subsection{Evaluation}
    \label{sec:env-eval}

    We evaluate our neural environment predictors on real-world network
    traces from the Puffer dataset.  The evaluation focuses on two
    complementary objectives: (1)~measuring raw prediction accuracy, and
    (2)~deriving formal statistical guarantees suitable for verification.
    We train models on individual daily traces and on a combined multi-day
    dataset to assess both in-distribution performance and generalisation
    across diverse network conditions.


    % - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
    \subsubsection{Metrics}
    \label{sec:env-metrics}

    We evaluate model reliability through statistical certification using
    exact binomial confidence intervals.  Because our goal is a verifiable
    environment model suitable for formal reasoning, we focus on deriving
    provable guarantees on prediction accuracy rather than traditional
    point-wise error metrics.

    A prediction is counted as a \emph{success} if
    $\lvert f(x) - f^{\ast}(x) \rvert \leq 0.10$---that is, the
    prediction error is within $10\%$ in normalised space.  Given the
    observed success rate on a test set, we compute the $90\%$
    Clopper--Pearson confidence interval $[L, U]$ on the true success
    probability.  We report the interval midpoint $(L + U)/2$ with error
    bars of width $(U - L)/2$ to visualise both the point estimate and the
    statistical uncertainty.  The lower bound $L$ provides our certified
    guarantee.


    % - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
    \subsubsection{Experimental Setup}
    \label{sec:env-setup}

    We trained two types of models:
    \begin{itemize}
        \item \textbf{Individual models:} $10$ models per architecture, each
            trained on $1{,}000$ samples from a single day and tested on
            $1{,}000$ held-out samples from the same day.  These models
            evaluate in-distribution performance.
        \item \textbf{Combined model:} one model per architecture trained on
            $10{,}000$ samples drawn from $10$ different days and tested on
            $9{,}000$ samples from $9$ different days.  This model
            evaluates cross-day generalisation.
    \end{itemize}
    All models use the architectures described in
    Section~\ref{sec:env-nn}, trained with the Adam optimiser and L1 loss.
    We evaluate both $2$-hidden-layer and $3$-hidden-layer networks.


    % - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
    \subsubsection{Results}
    \label{sec:env-results}

    \begin{figure}[t]
        \centering
        \begin{subfigure}[b]{0.48\columnwidth}
            \centering
            \includegraphics[width=\textwidth]{figures/2-layer.png}
            \caption{2-layer models}
            \label{fig:clopper2}
        \end{subfigure}
        \hfill
        \begin{subfigure}[b]{0.48\columnwidth}
            \centering
            \includegraphics[width=\textwidth]{figures/3-layer.png}
            \caption{3-layer models}
            \label{fig:clopper3}
        \end{subfigure}
        \caption{Clopper--Pearson certified accuracy for $2$-layer (left)
            and $3$-layer (right) models trained on individual daily traces
            and a combined multi-day dataset.  Error bars show confidence
            interval half-width.}
        \label{fig:env-cp}
    \end{figure}

    Figure~\ref{fig:env-cp} shows the $90\%$ Clopper--Pearson certified
    accuracies for both architectures.  Error bars indicate confidence
    interval half-width.

    \paragraph{Overall performance.}
    Across both architectures, certified training accuracies remain high
    (typically $0.98$--$0.995$), indicating that all models reliably fit
    their training distributions.  Test performance varies more
    substantially across individual traces, reflecting the inherent
    non-stationarity of real network environments.  Most models achieve
    certified test accuracies above $0.95$, demonstrating that the
    predictors generalise well within the $10\%$ error tolerance.

    \paragraph{Architecture comparison.}
    The $3$-layer models generally achieve tighter confidence intervals and
    slightly higher certified accuracies on traces from late October to
    early November.  For instance, on traces from October~30--31,~2024, the
    $3$-layer models reach certified accuracies of approximately $0.99$,
    compared to $0.985$--$0.99$ for $2$-layer models.  This suggests that
    additional capacity helps capture complex temporal patterns in network
    behaviour.

    However, deeper models also show larger degradation on challenging
    traces.  On September~20 and November~4,~2024, the $3$-layer models
    achieve certified accuracies of roughly $0.88$ and $0.85$,
    respectively---slightly lower than their $2$-layer counterparts.  This
    indicates mild overfitting to training distributions, a known
    limitation of deeper networks on heterogeneous data.

    \paragraph{Temporal stability.}
    Both architectures perform best on traces from late October through
    early November (e.g.\ October~28--November~1,~2024), achieving
    certified accuracies in the $0.96$--$0.99$ range with narrow confidence
    intervals.  These traces likely represent more stationary network
    conditions.  Performance degrades on earlier traces (mid-September),
    suggesting distribution shift or higher network variability during
    those periods.

    \paragraph{Combined model performance.}
    The combined model---trained on data from $10$ different days---achieves
    strong and stable certified accuracy.  The $2$-layer combined model
    achieves a lower bound of $0.9846$ with $90\%$ confidence
    (Section~\ref{sec:env-certification}), meaning at least $98.46\%$ of
    predictions have less than $10\%$ error.  Importantly, the combined
    model exhibits one of the tightest Clopper--Pearson intervals across
    all experiments, making it the most reliable candidate for use as a
    certified environment predictor in verification tasks.

    \paragraph{Key findings.}
    Our evaluation demonstrates the following:
    \begin{itemize}
        \item Both $2$-layer and $3$-layer models achieve $> 95\%$ certified
            accuracy under $10\%$ error tolerance on most traces.
        \item $3$-layer models provide higher peak accuracy but are more
            sensitive to distribution shift.
        \item $2$-layer models offer greater stability on challenging
            traces.
        \item Combined models trained on diverse daily traces provide the
            best balance of accuracy and confidence interval tightness.
    \end{itemize}
    These findings validate that a diverse training set and modest network
    capacity yield reliable, formally bounded environment predictors
    suitable for verifiable ABR control.


    % ---------------------------------------------------------------------------
    \subsection{Summary}
    \label{sec:env-summary}

    This chapter presented a statistically certified neural predictor for
    environment transitions in ABR streaming systems.  We formulated the
    ABR pipeline as a neuro-symbolic system and constructed a formally
    bounded approximation of the environment transition function using
    real-world network traces from the Puffer dataset.  By applying
    Clopper--Pearson confidence intervals, we derived rigorous statistical
    guarantees on prediction accuracy: our best model achieves less than
    $10\%$ normalised error on at least $98.46\%$ of inputs with $90\%$
    confidence.

    These results demonstrate that formally bounded environment models are
    both feasible and accurate enough for verification tasks.  The certified
    predictor $f^{\ast}$ addresses a critical prerequisite for verifying
    ML-based ABR controllers in closed-loop settings, providing the
    trustworthy symbolic component needed for principled reasoning about
    system behaviour.

    The certified predictor established here feeds directly into the
    end-to-end verification pipeline presented in
    Chapter~\ref{chap:verify}.  In that pipeline, the Pensieve phase
    partitions the input region into certified subregion--action pairs, and the environment
    phase propagates bounds through $f^{\ast}$ to obtain guaranteed
    next-state bounds.  The Clopper--Pearson certified accuracy derived in
    this chapter underpins the statistical compounding bound that quantifies
    how prediction error accumulates across multiple control rounds.

% =============================================================================
%  CHAPTER 5: END-TO-END QoE VERIFICATION PIPELINE
% =============================================================================

\section{End-to-End QoE Verification Pipeline}
\label{chap:verify}

    The previous two chapters established the building blocks for formal
    reasoning about Pensieve's closed-loop behaviour: a concrete threat
    model demonstrating adversarial vulnerability
    (Chapter~\ref{chap:attack}) and a statistically certified environment
    predictor $f^{\ast}$ (Chapter~\ref{chap:env}).  This chapter unifies
    them into an end-to-end verification pipeline that reasons about
    multi-round QoE properties of the complete neuro-symbolic control loop.

    The pipeline alternates between two phases at each control round~$t$:
    \begin{enumerate}
        \item \textbf{Pensieve phase.}  Given a bounded input region
            $[\ell_t, u_t]$, we use \texttt{auto\_LiRPA}~\cite{auto-lirpa}
            to compute output bounds for the Pensieve controller.  Where
            the bounds are too loose to certify a single dominant action,
            we split the input region into smaller subregions and re-verify
            each one.  Subregions where a single action dominates are
            directly certified; subregions that remain ambiguous at the
            maximum split depth are \emph{forked}---the same state bounds
            are paired with each remaining candidate action, creating
            one branch per action in the reachability tree.
        \item \textbf{Environment phase.}  For each subregion--action pair,
            we compose the subregion bounds with
            $f^{\ast}$ and propagate interval bounds through the
            environment predictor using LiRPA.  This yields guaranteed
            next-state bounds $[\ell_{t+1}, u_{t+1}]$ that become the
            input region for round $t{+}1$.
    \end{enumerate}
    Iterating these two phases for $T$ rounds produces a \emph{hierarchical
    reachability tree} (Section~\ref{sec:reachability-tree}), where each path
    from root to leaf represents a certified action sequence with
    guaranteed QoE bounds.  Figure~\ref{fig:pipeline-overview} illustrates
    this two-phase structure.

    \begin{figure}[t]
        \centering
        \includegraphics[width=0.95\linewidth]{figures/pipeline_overview_src.pdf}
        \caption{Overview of the two-phase verification pipeline.  At each
            round~$t$, the Pensieve phase applies LiRPA bound propagation
            and region splitting to produce certified actions~$a_t$, and
            the environment phase propagates bounds through the certified
            predictor $f^{\ast}$ to yield next-state bounds
            $[\ell_{t+1}, u_{t+1}]$.  Iterating across rounds builds a
            hierarchical reachability tree whose root-to-leaf paths are
            certified action sequences with guaranteed $\Delta$QoE bounds.}
        \label{fig:pipeline-overview}
    \end{figure}

    Section~\ref{sec:verify-formulation} formulates the ABR pipeline as a
    neuro-symbolic system and defines the verification target.
    Section~\ref{sec:verify-pensieve-phase} describes the Pensieve
    verification phase, including our input-region splitting strategy.
    Section~\ref{sec:reachability-tree} explains how the environment phase and
    multi-round composition produce a hierarchical reachability tree.
    Section~\ref{sec:verify-impl} describes the implementation optimisations
    that make the pipeline tractable in practice.
    Section~\ref{sec:verify-theory} presents theoretical soundness
    guarantees and the statistical compounding bound.
    Section~\ref{sec:verify-results} presents the reachability results.
    Section~\ref{sec:verify-summary} summarises the chapter.


    % ===========================================================================
    \subsection{Neuro-Symbolic System Formulation}
    \label{sec:verify-formulation}

    We model ABR streaming as a neuro-symbolic closed-loop system composed
    of two interacting components: (1)~a \emph{neural controller} that
    selects bitrates based on the current state, and (2)~an
    \emph{environment transition function} that determines how each bitrate
    decision influences future network and playback behaviour.  This
    decomposition captures both the expressive, data-driven decision logic
    of the controller and the structured evolution of the underlying system
    state.

    \paragraph{System dynamics.}
    Let $s_t$ denote the system state at time~$t$, consisting of recent
    throughput measurements, download times, buffer occupancy, past
    bitrates, available chunk sizes, and remaining chunks.  The neural
    controller selects a bitrate $a_t = \mathrm{NN}(s_t)$ based on this
    state.  The environment transition function~$f$ then maps
    $(s_t, a_t)$ to the next state:
    \begin{equation}
        s_{t+1} = f(s_t,\, a_t).
        \label{eq:verify-transition}
    \end{equation}
    The function $f$ embodies complex network characteristics---bandwidth
    fluctuations, network delay, chunk sizes---whose exact form is unknown.
    While we can observe state transitions in real traces, we cannot
    directly access or verify $f$ itself.  The certified predictor
    $f^{\ast}$ constructed in Chapter~\ref{chap:env} serves as our
    trustworthy approximation.

    \paragraph{QoE metric.}
    The standard Pensieve QoE model (Equation~\ref{eq:qoe}) combines
    bitrate utility, a rebuffering penalty, and a smoothness penalty.
    In our verification setting we set the rebuffering coefficient
    $\mu = 0$, reducing the per-step QoE to
    \begin{equation}
        \mathrm{QoE}(a_t, a_{t-1})
        \;=\; q(a_t)
            \;-\; \nu\, \lvert q(a_t) - q(a_{t-1}) \rvert,
        \label{eq:qoe-step}
    \end{equation}
    where $q(a_t)$ is the bitrate utility of the selected action and
    $\nu = 1$ penalises quality switches between consecutive chunks.
    We omit the rebuffering term because rebuffering depends on the
    ratio of chunk download time to chunk duration---a quantity that
    requires real-time network throughput, which is not available inside
    our bound-propagation framework.  The certified environment predictor
    $f^{\ast}$ produces bounded next-state estimates, not actual download
    times, so any rebuffering estimate would be vacuously loose.
    Dropping the term lets us compute tight, meaningful QoE bounds from
    the quantities the pipeline \emph{can} certify: the selected bitrate
    and the previous bitrate.

    We reason about \emph{changes} in QoE across control rounds rather
    than absolute levels.  The per-round QoE change is
    \begin{equation}
        \Delta\mathrm{QoE}_t
        \;=\; \mathrm{QoE}(a_t, a_{t-1})
            \;-\; \mathrm{QoE}(a_{t-1}, a_{t-2}),
        \label{eq:delta-qoe}
    \end{equation}
    where a positive value indicates improvement and a negative value
    indicates degradation.

    \paragraph{Verification target.}
    Algorithm~\ref{alg:neuro-symbolic} formalises the closed-loop
    interaction between the neural controller and the certified environment
    model.  The system tracks consecutive rounds in which
    $\Delta\text{QoE}$ falls below a minimum acceptable change
    threshold~$\alpha$.  The assertion at line~13 enforces that either
    the QoE change is non-degrading
    (i.e.\ $\Delta\text{QoE}_t \geq \alpha$), or the number of
    consecutive degradation rounds stays below a tolerance limit~$\beta$.
    This $\Delta\text{QoE}$ assertion detects sustained deterioration
    even when absolute QoE remains nominally acceptable, while still
    accommodating transient fluctuations under challenging network
    conditions.

    \begin{algorithm}[t]
        \caption{Neuro-symbolic formulation of ABR streaming}
        \label{alg:neuro-symbolic}
        \begin{algorithmic}[1]
            \Require Initial state $s_0$, Pensieve controller $\mathrm{NN}$,
                    certified predictor $f^{\ast}$, $\Delta$QoE threshold $\alpha$,
                    violation tolerance $\beta$
            \State $a \gets \mathrm{NN}(s_0)$
                \Comment{Initial action from Pensieve}
            \State $s \gets f^{\ast}(s_0,\, a)$
                \Comment{Environment transition via certified predictor}
            \State $\text{QoE}_{\text{prev}} \gets \text{QoE}(s_0,\, a)$
                \Comment{Record initial QoE}
            \State $\mathit{failure\_counter} \gets 0$
            \While{\textbf{true}}
                \State $a \gets \mathrm{NN}(s)$
                    \Comment{Pensieve outputs next bitrate decision}
                \State $s \gets f^{\ast}(s,\, a)$
                    \Comment{Update state using certified environment model}
                \State $\Delta\text{QoE} \gets \text{QoE}(s,\, a) - \text{QoE}_{\text{prev}}$
                    \Comment{Per-round QoE change}
                \State $\text{QoE}_{\text{prev}} \gets \text{QoE}(s,\, a)$
                \If{$\Delta\text{QoE} < \alpha$}
                    \State $\mathit{failure\_counter} \gets
                        \mathit{failure\_counter} + 1$
                \Else
                    \State $\mathit{failure\_counter} \gets 0$
                \EndIf
                \State \textbf{assert}
                    $(\Delta\text{QoE} \geq \alpha) \;\lor\;
                        (\mathit{failure\_counter} < \beta)$
            \EndWhile
        \end{algorithmic}
    \end{algorithm}

    The verification goal is to determine, for a given initial state
    region, whether the $\Delta\text{QoE}$ assertion in
    Algorithm~\ref{alg:neuro-symbolic} holds across multiple control
    rounds, or to identify concrete action sequences that lead to
    sustained QoE degradation.


    % ===========================================================================
    \subsection{Pensieve Verification Phase}
    \label{sec:verify-pensieve-phase}

    The Pensieve phase takes a bounded input region $[\ell, u] \subseteq
    \mathbb{R}^{48}$ and determines which of Pensieve's six bitrate actions
    (Section~\ref{subsec:streaming}) is provably dominant over the entire
    region.  We first compute output bounds using
    LiRPA (Section~\ref{sec:bg-lirpa}), then apply input-region splitting
    when the initial bounds are insufficient.


    % ---------------------------------------------------------------------------
    \subsubsection{Bound Computation via LiRPA}
    \label{sec:verify-lirpa-usage}

    Given the input region $[\ell, u]$, we invoke
    \texttt{auto\_LiRPA}~\cite{auto-lirpa} with CROWN-Optimised bound
    propagation (Section~\ref{sec:bg-lirpa}) to compute lower and upper
    bounds $[\underline{y}_i, \overline{y}_i]$ for each output logit $y_i$,
    $i \in \{0, \ldots, 5\}$.

    An action $a_j$ is \emph{provably dominant} over $[\ell, u]$ if its
    lower bound exceeds the upper bounds of all other actions:
    \begin{equation}
        \underline{y}_j > \overline{y}_i
        \quad \forall\, i \neq j.
        \label{eq:dominant}
    \end{equation}
    When this condition holds, we call $[\ell, u]$ a \emph{safe region} for
    action~$a_j$: every input in the region is guaranteed to produce the
    same controller decision.

    If no single action satisfies Equation~\ref{eq:dominant}, the output
    bounds are too loose---the relaxation introduced by LiRPA leaves
    ambiguity about which action Pensieve would select.  In this case, we
    refine the analysis by splitting the input region.


    % ---------------------------------------------------------------------------
    \subsubsection{Input-Region Splitting}
    \label{sec:verify-splitting}

    When LiRPA cannot certify a dominant action for the full input region,
    the output bounds for multiple actions overlap: no single action
    satisfies the dominance condition (Equation~\ref{eq:dominant}), so
    $\Delta\mathrm{QoE}$ cannot be attributed to a single, known action.
    We resolve this ambiguity by bisecting the input region along the
    throughput dimension and re-verifying each half independently.
    Smaller subregions yield tighter LiRPA bounds, which can separate
    previously overlapping actions.  Figure~\ref{fig:splitting} illustrates
    the two outcomes: a subregion whose bounds are now tight enough to
    certify a single dominant action produces a clean $\Delta\mathrm{QoE}$
    value; a subregion that remains ambiguous after exhausting the split
    budget is handled by propagating the same state bounds forward
    independently for each candidate action.

    \begin{figure}[t]
        \centering
        \includegraphics[width=0.98\linewidth]{figures/bound_splitting_src.pdf}
        \caption{Input-region splitting for action certification and
            $\Delta\mathrm{QoE}$ computation.
            \textbf{(a)}~For the full region $[\ell, u]$, LiRPA output
            bounds for $a_3$ and $a_4$ overlap: the dominant action
            is ambiguous, so $\Delta\mathrm{QoE}$ cannot be determined.
            The region is bisected on the throughput bound, producing
            $[\ell_1, u_1]$ and $[\ell_2, u_2]$.
            \textbf{(b)}~In $[\ell_1, u_1]$ the tighter bounds certify
            $a_3$ as uniquely dominant, yielding a single
            $\Delta\mathrm{QoE}$ value for that subregion.
            \textbf{(c)}~In $[\ell_2, u_2]$ ambiguity persists after
            the split budget is exhausted; the same state bounds are
            propagated forward separately for each candidate action
            ($a_3$ and $a_4$), creating two branches in the
            reachability tree.}
        \label{fig:splitting}
    \end{figure}

    \paragraph{Splitting strategy.}
    Our splitting procedure operates as follows.  Given an input region
    $[\ell, u]$ for which no dominant action can be certified:
    \begin{enumerate}
        \item \textbf{Throughput bisection.}  We always split on the
            most recent throughput feature, bisecting at its midpoint:
            \begin{equation}
                m = \bigl(\ell_{\mathrm{tp}} + u_{\mathrm{tp}}\bigr) / 2,
                \label{eq:split-dim}
            \end{equation}
            producing two subregions $[\ell_1, u_1]$ and $[\ell_2, u_2]$
            that differ only in the most recent throughput bound.
            We always split on this single dimension because throughput
            is derived from chunk size divided by chunk download time,
            making it consistently the widest dimension in the input
            region.
        \item \textbf{Re-verification.}  Each subregion is independently
            verified using LiRPA.  If a subregion admits a dominant
            action, it is marked as a \emph{safe region} with a certified
            $\Delta\mathrm{QoE}$ value.  Otherwise, it is placed back in
            the queue for further splitting.
        \item \textbf{Termination and forking.}  The procedure terminates
            when all subregions are certified or a maximum split
            depth~$D$ is reached (we use $D = 8$).  Subregions that
            remain ambiguous at depth~$D$ are not discarded; instead,
            the same state bounds are propagated forward once per
            candidate action, creating parallel branches in the
            reachability tree and ensuring no reachable state is
            omitted.
    \end{enumerate}

    This approach differs from classical branch-and-bound (BaB) methods
    used in complete neural network verification~\cite{bab} in two key
    respects.  First, BaB typically branches on \emph{intermediate}
    neurons (e.g.\ splitting on whether a ReLU is active or inactive),
    whereas our strategy splits on \emph{input} dimensions.  Input-space
    splitting is natural in our setting because the subregions produced
    have direct physical meaning---they correspond to contiguous regions of
    the ABR state space---and each subregion--action pair is subsequently
    fed into the environment phase as a self-contained input.  Second, our
    goal is not to prove a single binary property but to \emph{partition}
    the input space into certified regions, each paired with an action,
    building the foundation for multi-round QoE analysis.


    % ===========================================================================
    \subsection{Multi-Round Reachability Analysis}
    \label{sec:reachability-tree}
    \label{sec:verify-env-phase}

    Composing the Pensieve and environment phases across $T$ rounds
    produces a \emph{hierarchical reachability tree}.
    Figure~\ref{fig:reachability-tree} shows an example tree for $T = 5$ rounds.

    \paragraph{Environment phase.}
    For each subregion--action pair $([\ell, u],\, a_j)$ produced by the
    Pensieve phase, the environment phase computes guaranteed next-state
    bounds.  The inputs to $f^{\ast}$ are constructed by combining the
    state bounds $[\ell, u]$ with the action~$a_j$.  We then
    propagate these bounds through the environment predictor $f^{\ast}$
    using LiRPA, obtaining
    \begin{equation}
        [\ell_{t+1},\, u_{t+1}]
        \supseteq \bigl\{f^{\ast}(s, a_j) : s \in [\ell, u]\bigr\}.
        \label{eq:env-bounds}
    \end{equation}
    The over-approximation is sound: the true next state for any
    $s \in [\ell, u]$ under action $a_j$ is guaranteed to lie within
    $[\ell_{t+1}, u_{t+1}]$.  The resulting next-state bounds serve as the
    input region for the Pensieve phase of the subsequent round.  Because
    the environment phase introduces its own over-approximation (from LiRPA
    relaxation of $f^{\ast}$), and because $f^{\ast}$ is itself an
    approximation of the true dynamics~$f$ (with certified accuracy from
    Chapter~\ref{chap:env}), the next-state bounds grow with each round.
    Section~\ref{sec:verify-theory} formalises how these two sources of
    imprecision compound across rounds.

    \begin{figure}[t]
        \centering
        % TODO: insert the reachability tree figure (Image 1 from the
        % screenshots).
        % \includegraphics[width=\linewidth,alt={Hierarchical verification
        %   tree for four control rounds.  Green nodes are certified
        %   subregion--action pairs; cream nodes are subregions undergoing
        %   further splitting.  Each level alternates between Pensieve and
        %   environment phases.}]{figures/verification_tree.pdf}
        \caption{Example hierarchical reachability tree for five control
            rounds.  Each level alternates between Pensieve-phase splitting
            (producing certified subregion--action pairs shown in green) and environment-phase
            bound propagation (producing next-state regions shown in cream).
            The tree fans out as subregions multiply through splitting, then
            each branch propagates independently through subsequent rounds.}
        \label{fig:reachability-tree}
    \end{figure}

    \paragraph{Tree structure.}
    The root node represents the initial input region $[\ell_0, u_0]$,
    typically derived from a SpecTRA specification
    (Section~\ref{sec:bg-spectra}).  At each odd level (Pensieve phase),
    the region is split into subregions, each paired with a certified
    action (a single subregion may appear multiple times if it was forked
    across candidate actions).  At each even level (environment phase),
    each subregion--action pair produces a next-state bound via
    $f^{\ast}$.  The children of an environment node are the
    subregion--action pairs produced by the next Pensieve phase applied
    to that next-state bound.

    \paragraph{Action sequences.}
    Each root-to-leaf path through the tree defines a \emph{certified
    action sequence}: a series of bitrate decisions
    $a_0 \to a_1 \to \cdots \to a_{T-1}$ for which every intermediate
    state is guaranteed to lie within computed bounds.  The set of all such
    paths enumerates the possible behaviours of the controller over $T$
    rounds, subject to the over-approximation introduced by LiRPA and the
    statistical certification of $f^{\ast}$.

    \paragraph{QoE analysis.}
    For each certified action sequence, we compute $\Delta\text{QoE}$
    bounds (Equation~\ref{eq:delta-qoe}) using the bitrate values associated
    with the certified actions and the guaranteed state bounds (which
    determine rebuffering and quality-switch contributions).  A sequence is
    flagged as potentially problematic if the lower bound on
    $\Delta\text{QoE}$ at any round falls below the degradation threshold
    $\alpha$ from Algorithm~\ref{alg:neuro-symbolic}.


    % ===========================================================================
    \subsection{Implementation and Optimisations}
    \label{sec:verify-impl}

    The verification pipeline as described is computationally demanding:
    each call to \texttt{compute\_bounds} carries significant overhead from
    CROWN-Optimised bound propagation, and the branching structure of the
    reachability tree can produce thousands of subregions per round.  Three
    targeted optimisations make the pipeline tractable in practice:
    a single-pass \textsc{AllDominance} layer that eliminates redundant bound
    propagation calls (Section~\ref{sec:verify-alldominance}), batched
    verification that amortises per-call overhead across many subregions
    (Section~\ref{sec:verify-batching}), and multi-GPU parallelism that
    processes independent regions concurrently across four H100 devices
    (Section~\ref{sec:verify-multigpu}).


    % ---------------------------------------------------------------------------
    \subsubsection{Single-Pass Dominance via \textsc{AllDominance}}
    \label{sec:verify-alldominance}

    A na\"{i}ve implementation of the dominance check
    (Equation~\ref{eq:dominant}) invokes \texttt{compute\_bounds} once per
    candidate action: for each $a_j$, bounds are propagated through the
    network and checked against the remaining actions.  For Pensieve's six
    actions this incurs six separate bound-propagation passes per region.

    We eliminate this redundancy by appending a fixed linear layer to the
    Pensieve actor before wrapping it in \texttt{BoundedModule}.  Define the
    \emph{dominance matrix} $M \in \mathbb{R}^{30 \times 6}$, where each of
    the $6 \times 5 = 30$ rows encodes a pairwise difference $y_k - y_i$
    for a distinct pair $(k,i)$ with $k \neq i$.  The composite model
    \begin{equation}
        g(x) = M \cdot \mathrm{NN}(x)
        \label{eq:alldominance}
    \end{equation}
    produces a $30$-dimensional output in a single forward pass.  Because $M$
    has no trainable parameters, LiRPA propagates bounds through it
    analytically with negligible overhead.  The dominance check then reduces
    to a single comparison against the lower-bound vector
    $\underline{g}(x) \in \mathbb{R}^{30}$ returned by
    \texttt{compute\_bounds}:
    \begin{equation}
        \text{action } a_k \text{ is dominant}
        \iff
        \underline{g}(x)_{j} \geq 0
        \quad \forall\, j \in \{k(a_{\dim}-1),\, \ldots,\, (k+1)(a_{\dim}-1) - 1\},
        \label{eq:alldominance-check}
    \end{equation}
    where $a_{\dim} = 6$.  All six dominance decisions are therefore resolved
    in a single \texttt{compute\_bounds} call---a $6\times$ reduction in the
    dominant computational cost of the inner verification loop.


    % ---------------------------------------------------------------------------
    \subsubsection{Batched Bound Propagation}
    \label{sec:verify-batching}

    CROWN-Optimised bound propagation incurs a fixed per-call overhead
    (coefficient optimisation, graph traversal, GPU kernel launch) that is
    amortised across all elements in the batch.  Processing subregions one at
    a time therefore wastes the vast majority of available GPU throughput.

    We exploit the batch dimension of \texttt{auto\_LiRPA}: given $B$
    subregions with bounds $(\ell^{(1)}, u^{(1)}), \ldots,
    (\ell^{(B)}, u^{(B)})$, each of shape $1 \times 6 \times 8$, we stack
    them along the batch axis to form a single $B \times 6 \times 8$
    \texttt{BoundedTensor} and issue one \texttt{compute\_bounds} call
    returning a $B \times 30$ lower-bound tensor.  Dominance is then checked
    independently for each element of the batch.

    We cap the batch size at $B = 256$ subregions per call.  At this size
    the H100 GPU's memory bandwidth is saturated while device memory
    remains within capacity.  For a queue of $N$ pending subregions, the
    total number of \texttt{compute\_bounds} calls is therefore
    $\lceil N / 256 \rceil$ rather than $N$.


    % ---------------------------------------------------------------------------
    \subsubsection{Multi-GPU Parallelism}
    \label{sec:verify-multigpu}

    The Pensieve and environment phases for distinct regions within a given
    round are mutually independent: no region's verification result depends on
    another's.  We exploit this structure by distributing regions across four
    NVIDIA H100 GPUs using Python's \texttt{ThreadPoolExecutor}.

    Three worker threads, each pinned to a dedicated device
    (\texttt{cuda:1}--\texttt{cuda:3}), own independent copies of the
    \texttt{BoundedModule} instances initialised once at thread start-up via
    an initialiser function.  A fourth device (\texttt{cuda:0}) is reserved
    for the main thread's lightweight logging passes---a single forward call
    per round to record logit bounds for the verification report.  Because
    all \texttt{BoundedModule} copies are thread-local, there is zero
    inter-thread synchronisation during bound propagation: each worker
    processes its assigned regions entirely on its own device.

    At each round the main thread partitions the current region list into
    per-worker tasks, submits them via \texttt{executor.submit}, and collects
    results with \texttt{future.result}.  After all workers return, the main
    thread renumbers the workers' local node identifiers to globally unique
    IDs---an $\mathcal{O}(N)$ pass over the returned records---before logging
    the completed tree level and dispatching the next round.  This design
    avoids both GIL contention (CUDA kernels release the GIL) and
    inter-process serialisation overhead that would arise in a multiprocessing
    design.


    % ===========================================================================
    \subsection{Theoretical Guarantees}
    \label{sec:verify-theory}


    % ---------------------------------------------------------------------------
    \subsubsection{Soundness}
    \label{sec:verify-soundness}

    The verification pipeline inherits the soundness of its two components.

    \begin{theorem}[Pensieve-phase soundness]
    \label{thm:pensieve-sound}
    If LiRPA certifies action $a_j$ as dominant over region $[\ell, u]$
    (Equation~\ref{eq:dominant}), then for every $s \in [\ell, u]$,
    $\arg\max_i \mathrm{NN}(s)_i = j$.
    \end{theorem}

    \begin{proof}
    By the soundness of LiRPA (Section~\ref{sec:bg-lirpa}), for every
    $s \in [\ell, u]$ and every output index~$i$, the true logit satisfies
    $\underline{y}_i \leq \mathrm{NN}(s)_i \leq \overline{y}_i$.  If
    $\underline{y}_j > \overline{y}_i$ for all $i \neq j$, then
    $\mathrm{NN}(s)_j > \mathrm{NN}(s)_i$ for all $i \neq j$, so action
    $a_j$ is selected.
    \end{proof}

    \begin{theorem}[Environment-phase soundness]
    \label{thm:env-sound}
    If LiRPA computes next-state bounds $[\ell_{t+1}, u_{t+1}]$ by
    propagating $[\ell_t, u_t]$ and action $a_j$ through $f^{\ast}$, then
    for every $s \in [\ell_t, u_t]$,
    $f^{\ast}(s, a_j) \in [\ell_{t+1}, u_{t+1}]$.
    \end{theorem}

    \begin{proof}
    This follows directly from the soundness of LiRPA applied to
    $f^{\ast}$, which is a neural network with known architecture and
    weights.
    \end{proof}

    \begin{corollary}[Pipeline soundness]
    \label{cor:pipeline-sound}
    Every certified action sequence in the reachability tree is a valid
    execution of the system $s_{t+1} = f^{\ast}(s_t, a_t)$,
    $a_t = \mathrm{NN}(s_t)$, for all $s_0 \in [\ell_0, u_0]$.
    \end{corollary}

    \begin{proof}
    By induction on the number of rounds.  The base case ($T = 1$) is
    Theorem~\ref{thm:pensieve-sound}.  For the inductive step, suppose
    all states at round~$t$ lie within $[\ell_t, u_t]$.  By
    Theorem~\ref{thm:pensieve-sound}, the certified action $a_t$ is
    selected for every such state.  By Theorem~\ref{thm:env-sound},
    the next state $f^{\ast}(s_t, a_t) \in [\ell_{t+1}, u_{t+1}]$,
    so the invariant holds at round $t{+}1$.
    \end{proof}

    Note that these guarantees are with respect to the \emph{approximate}
    environment $f^{\ast}$, not the true dynamics~$f$.  The following
    subsection quantifies the gap.


    % ---------------------------------------------------------------------------
    \subsubsection{Statistical Compounding Bound}
    \label{sec:verify-compounding}

    The certified predictor $f^{\ast}$ satisfies
    $\Pr(\lvert f(x) - f^{\ast}(x) \rvert \leq \epsilon) \geq p$
    with confidence $1 - \delta$ (Chapter~\ref{chap:env}), where
    $p = 0.9846$ and $\epsilon = 0.10$ in our experiments.  Over $T$
    rounds, the probability that \emph{all} environment predictions remain
    within tolerance is bounded by:

    \begin{theorem}[Compounding bound]
    \label{thm:compounding}
    Under the i.i.d.\ assumption on successive environment predictions,
    the probability that every round's prediction incurs less than
    $\epsilon$ normalised error satisfies
    \begin{equation}
        \Pr\!\Bigl(\bigcap_{t=1}^{T}
            \lvert f(x_t) - f^{\ast}(x_t) \rvert \leq \epsilon\Bigr)
        \;\geq\; p^T.
        \label{eq:compounding}
    \end{equation}
    \end{theorem}

    \begin{proof}
    Each round's prediction is modelled as an independent Bernoulli trial
    with success probability at least $p$.  The probability that all $T$
    trials succeed is at least $p^T$ by independence.
    \end{proof}

    For $T = 5$ rounds and $p = 0.9846$, the compounding bound gives
    $p^5 \geq 0.9246$: with $90\%$ confidence, at least $92.46\%$ of
    five-round trajectories have all environment predictions within
    $10\%$ normalised error.

    \paragraph{Interpretation.}
    The compounding bound quantifies how the irreducible gap between
    $f^{\ast}$ and $f$ accumulates across rounds.  It does \emph{not}
    account for the LiRPA over-approximation error (which is deterministic
    and captured by the interval widths), but rather the statistical
    uncertainty about whether $f^{\ast}$ faithfully represents the true
    dynamics at each step.  The two sources of imprecision are
    complementary: LiRPA bounds grow geometrically with rounds (a
    deterministic effect), while the compounding bound degrades
    multiplicatively (a probabilistic effect).

    \paragraph{Limitations of the i.i.d.\ assumption.}
    The independence assumption in Theorem~\ref{thm:compounding} is an
    idealisation.  In practice, successive states are correlated because
    each state is derived from the previous one via $f^{\ast}$.  The bound
    $p^T$ is therefore a first-order approximation.  Tighter bounds could be
    obtained by modelling the Markovian structure of the state sequence, but
    this is left to future work.


    % ===========================================================================
    \subsection{Reachability Results}
    \label{sec:verify-results}


    % ---------------------------------------------------------------------------
    \subsubsection{Experimental Setup}
    \label{sec:verify-setup}

    We evaluate the pipeline on $10$ distinct initial input regions, each
    derived from a different SpecTRA specification
    (Section~\ref{sec:bg-spectra}).  For each initial region, we run the
    two-phase pipeline for $T = 5$ control rounds.  The Pensieve phase uses
    CROWN-Optimised bound propagation via
    \texttt{auto\_LiRPA}~\cite{auto-lirpa}.  The environment phase uses the
    $2$-layer combined model from Chapter~\ref{chap:env} as the certified
    predictor~$f^{\ast}$.  Splitting is parallelised across four NVIDIA H100
    GPUs as described in Section~\ref{sec:verify-impl}.

    For each round of each specification, we compute:
    \begin{itemize}
        \item the number of subregion--action pairs produced by the Pensieve phase,
        \item the set of certified actions and their distribution across
            subregions,
        \item the width of the next-state bounds produced by the environment
            phase,
        \item the per-round $\Delta$QoE: the change in QoE bounds relative
            to the previous round.
    \end{itemize}


    % ---------------------------------------------------------------------------
    \subsubsection{Metrics}
    \label{sec:verify-metrics}

    We report the following metrics:
    \begin{itemize}
        \item \textbf{Verification rate:} the fraction of the initial input
            region that is partitioned into certified subregion--action
            pairs (including forked subregions paired with each
            candidate action).
        \item \textbf{Action distribution:} the set of distinct certified
            actions across subregion--action pairs, indicating how sensitive
            Pensieve's decision is to small state variations within the
            specification region.
        \item \textbf{Bound growth:} the ratio of next-state interval width
            to current-state interval width, measuring how quickly
            over-approximation accumulates across rounds.
        \item \textbf{$\Delta$QoE per round:} the change in QoE bounds
            between consecutive rounds, computed from the certified action
            and the state bounds.  Large negative values indicate rounds
            where the controller is certified to make a decision that
            substantially degrades quality.
    \end{itemize}


    % ---------------------------------------------------------------------------
    \subsubsection{Results}
    \label{sec:verify-results-detail}

    \paragraph{Reachability tree structure.}
    Figure~\ref{fig:reachability-tree} shows a representative reachability tree
    for one of the $10$ specifications over $5$ rounds.  The tree exhibits
    the expected fan-out pattern: the initial region is split into
    approximately $8$--$10$ subregion--action pairs in the first Pensieve phase,
    each producing a next-state region via the environment phase, which is
    then further split in subsequent rounds.  By round~$5$, the tree
    contains several hundred leaf nodes, each representing a certified
    action sequence.

    \paragraph{QoE degradation paths.}
    Across the $10$ specifications, we identify multiple action sequences
    that lead to substantial QoE drops at particular rounds.  These paths
    correspond to scenarios where the certified action at one round selects
    a high bitrate (e.g.\ $a_4 = 2850$~kbps or $a_5 = 4300$~kbps),
    followed by a sharp drop to a low bitrate ($a_0 = 300$~kbps or
    $a_1 = 750$~kbps) in a subsequent round.  The resulting quality-switch
    penalty dominates the QoE calculation, producing large negative
    $\Delta$QoE values.  Representative degradation sequences include:
    \begin{itemize}
        \item $a_4 \to a_2 \to a_5 \to a_2$: repeated drops from high to
            low bitrate across four rounds, incurring large quality-switch
            penalties.
        \item $a_4 \to a_1, a_2 \to a_4 \to a_5 \to a_4, a_5$: a
            five-round oscillation between high and low actions, with
            branching at rounds~2 and~5 reflecting subregion-dependent
            behaviour.
        \item $a_4 \to a_1, a_0, a_2 \to a_2, a_4 \to a_5 \to a_4, a_5
            \to a_1, a_4, a_5$: wide action spread in early rounds,
            indicating high controller sensitivity to small state
            variations within the specification region.
    \end{itemize}
    Here $a_i$ denotes the $i$-th bitrate action in Pensieve's action set
    $\{300, 750, 1200, 1850, 2850, 4300\}$~kbps (indexed from~$0$).

    These findings are consistent with the adversarial vulnerability
    established in Chapter~\ref{chap:attack}: Pensieve is sensitive to
    small state perturbations that can shift the selected action across
    bitrate levels.  The verification pipeline makes this sensitivity
    precise and exhaustive: rather than sampling individual adversarial
    examples, we certify \emph{all} possible behaviours within each
    specification region.

    \paragraph{Bound growth across rounds.}
    The interval width of next-state bounds grows with each round due to
    cumulative LiRPA over-approximation.  In our experiments, the
    average bound width approximately doubles per round for the first
    three rounds, then grows more slowly as the subregion splitting in
    the Pensieve phase compensates by producing narrower certified regions.
    After $5$ rounds, the bounds remain tight enough for meaningful QoE
    analysis in the majority of specifications, though a small number
    of leaf nodes have bounds too wide to draw definitive conclusions.

    \paragraph{Statistical compounding.}
    Applying the compounding bound (Theorem~\ref{thm:compounding}) with
    $p = 0.9846$ and $T = 5$, we obtain a lower bound of $0.9246$ on
    the probability that all five environment predictions fall within
    $10\%$ normalised error.  This means that the QoE guarantees derived
    from the reachability tree hold for at least $92.46\%$ of real
    trajectories (with $90\%$ confidence), assuming the i.i.d.\ condition
    discussed in Section~\ref{sec:verify-compounding}.

    % TODO: Add a table or figure summarising per-round $\Delta$QoE
    % across all 10 specifications, highlighting the rounds and action
    % transitions that produce the largest QoE drops.

    % \begin{table}[t]
    %     \centering
    %     \caption{Per-round $\Delta$QoE for representative action sequences
    %         across $10$ specifications.  Negative values indicate QoE
    %         degradation.  Bold entries mark rounds with $\Delta$QoE below
    %         $-1.0$.}
    %     \label{tab:delta-qoe}
    %     % TODO: fill in with actual experimental data
    % \end{table}


    % ===========================================================================
    \subsection{Summary}
    \label{sec:verify-summary}

    This chapter presented an end-to-end verification pipeline for
    reasoning about multi-round QoE properties of the Pensieve neural ABR
    controller operating in a closed-loop neuro-symbolic system.  The
    pipeline alternates between a Pensieve phase---which uses LiRPA bound
    propagation and input-region splitting to partition the state space into
    certified subregion--action pairs---and an environment phase---which
    propagates bounds through the certified predictor $f^{\ast}$ to obtain
    guaranteed next-state bounds.  Iterating for five control rounds builds
    a hierarchical reachability tree whose paths enumerate all certified
    action sequences.

    Three implementation optimisations make the pipeline tractable at scale:
    a single-pass \emph{AllDominance} layer that resolves all six dominance
    decisions in one \texttt{compute\_bounds} call
    (Section~\ref{sec:verify-alldominance}); batched bound propagation that
    amortises per-call overhead across up to 256 subregions
    (Section~\ref{sec:verify-batching}); and multi-GPU parallelism across
    four NVIDIA H100 devices using thread-local model copies with no
    inter-thread synchronisation (Section~\ref{sec:verify-multigpu}).

    We established three formal guarantees: Pensieve-phase soundness
    (Theorem~\ref{thm:pensieve-sound}), environment-phase soundness
    (Theorem~\ref{thm:env-sound}), and a statistical compounding bound
    (Theorem~\ref{thm:compounding}) quantifying how environment prediction
    error accumulates across rounds.  Experimental evaluation on $10$
    SpecTRA-derived specifications demonstrated that the pipeline can
    identify concrete action sequences leading to significant QoE
    degradation, providing exhaustive coverage that complements the
    sampling-based adversarial analysis of Chapter~\ref{chap:attack}.

    Together with the adversarial vulnerability study
    (Chapter~\ref{chap:attack}) and the certified environment model
    (Chapter~\ref{chap:env}), this pipeline completes the three-part
    framework introduced in Chapter~\ref{chap:intro}: exposing
    vulnerabilities, certifying the environment, and formally verifying
    closed-loop QoE properties.


\end{document}